/** Height of a single script line row in the player scroll view (px) */
export const SCRIPT_LINE_HEIGHT = 44;

/** Scale factor applied to the mandala lotus during the breathing animation when playing */
export const BREATHING_SCALE_ACTIVE = 1.04;

/** Threshold (ms) before track end that counts as a natural completion */
export const COMPLETION_THRESHOLD_MS = 1000;

/** Duration (ms) of one full outer mandala rotation loop */
export const MANDALA_OUTER_DURATION_MS = 20000;

/** Duration (ms) of one full middle mandala rotation loop */
export const MANDALA_MIDDLE_DURATION_MS = 15000;

/** Duration (ms) of one half-cycle of the glow pulse animation */
export const GLOW_PULSE_DURATION_MS = 1500;

/** Duration (ms) of the breathing scale animation half-cycle */
export const BREATHE_DURATION_MS = 2000;

/** Maximum number of consecutive days checked when computing the prayer streak */
export const STREAK_MAX_DAYS = 365;

/** Minimum drag-progress delta required to confirm a seek gesture */
export const SEEK_CONFIRM_THRESHOLD = 0.01;
